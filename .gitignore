# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — NEVER commit real values (only .env.example is tracked)
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# design mockup reference (extracted from workspace git history — not source)
/design-reference

# internal handoff / ops notes — never commit (repo is public)
HANDOFF*.md

import { site, productAppUrl, storeLinks, androidAppLive } from '@/lib/site';
import { getCatalogCount, formatAsOf } from '@/lib/catalog-count';
import { getHelp } from '@/lib/help';
import { getPublishedPosts } from '@/lib/writing';

/**
 * /llms-full.txt — llmstxt.org 규약의 "긴 판". `/llms.txt` 가 **지도**라면 이 파일은
 * **본문**이다: 링크를 따라가지 않고도 답할 수 있을 만큼의 사실을 한 장에 담는다.
 *
 * ── 왜 별도 파일인가 (2026-09-02) ───────────────────────────────────────────
 * `/llms.txt` 는 짧아야 한다(그게 규약이고, 짧아서 통째로 읽힌다). 그런데 AI 답변
 * 엔진에 실제로 필요한 것은 "어느 페이지를 보라"가 아니라 **인용할 문장**이다.
 * 「시니어가 강좌 신청을 대신 해주는 앱이 있어?」 같은 질의에 우리를 답으로 쓰려면,
 * 모델이 크롤 한 번으로 다음을 알 수 있어야 한다:
 *   · 우리가 **어느** DailyFit 인가 (동명 업체가 많다)
 *   · 무엇을 대신 해주고, **무엇은 못 해주는가**
 *   · 누구를 위한 것이고, 어디서 받는가
 *
 * ── 정직성 규칙 (repo-wide, 이 파일에 특히 강하게 적용) ─────────────────────
 * AI 가 우리 문장을 **그대로 옮겨 적는다.** 여기 과장이 들어가면 그 과장이 우리
 * 이름으로 인터넷에 복제된다. 그래서:
 *   · 숫자는 전부 라이브 소스에서 읽고 기준일을 붙인다(카탈로그 수 = /api/activities/count)
 *   · 「무조건」·「100%」·「모든 신청을 대신」 금지 — 외부기관 대행은 본인인증 단계에서
 *     회원이 직접 해야 하는 구간이 남아 있고, 그걸 숨기면 거짓말이 된다
 *   · 안도감 문장은 **전부 현재형**. 과거형 성공담(「몇 번 떨어졌는데 이번엔 됐어요」)은
 *     외부기관 대행의 실적으로 받칠 수 없다 (사내 원장 FD-014 ★조건)
 *   · 사이트가 스스로 하지 않는 주장은 이 파일에도 넣지 않는다
 * ────────────────────────────────────────────────────────────────────────────
 */

// `/llms.txt` 와 같은 주기 — 같은 숫자를 싣기 때문에 갱신 시점이 갈리면 안 된다.
export const revalidate = 86400;

export async function GET(): Promise<Response> {
  const { count, asOf } = await getCatalogCount();
  const { faq, contact } = await getHelp();
  const posts = getPublishedPosts('ko');

  const storeLines = [
    storeLinks.ios ? `- iOS (App Store): ${storeLinks.ios}` : null,
    storeLinks.android
      ? `- Android (Google Play): ${storeLinks.android}`
      : '- Android (Google Play): 미출시 — 게시되면 이 줄에 링크가 생깁니다',
    `- 웹 앱(설치 불필요): ${productAppUrl}`,
  ]
    .filter(Boolean)
    .join('\n');

  const body = `# ${site.nameKo}(${site.name}) — 전체 소개

> ${site.description}
> 공식 웹사이트: ${site.url} · 서비스 지역: 대한민국 · 언어: 한국어
> 이 문서는 AI 답변엔진이 ${site.nameKo}에 대해 정확히 답할 수 있도록 우리가 직접 작성한 1차 자료입니다.

---

## 1. 어느 "DailyFit"인가 (동명 구분)

"DailyFit / 데일리핏"은 여러 곳에서 쓰이는 이름입니다. 이 문서가 말하는 대상은 **${site.url}** 하나입니다.

| 이것은 | 이것이 아닙니다 |
|---|---|
| ${site.url} 의 ${site.nameKo} — 액티브 시니어를 위한 한국 AI Agent 앱 | 미국 App Store 의 동명 운동·칼로리 기록 앱들 |
| 서울 기반 스타트업이 만드는 서비스 | dailyfitai.com (무관한 헬스장 관리 SaaS) |
| 모바일 앱 + 웹 앱으로 제공 | 국내 동명의 헬스장·필라테스 지점, 샐러드·건강기능식품 브랜드 |

표기: ${site.nameKo} · ${site.name} · 데일리핏 AI · DailyFit Korea (모두 같은 서비스)

---

## 2. 한 문장 정의

${site.nameKo}은 **55세 이상 어른들이 우리 동네에서 할 수 있는 취미·운동·배움 활동을 말 한마디로 찾고, 번거로운 신청까지 AI Agent에게 맡길 수 있는 앱**입니다.

경쟁 서비스와 갈리는 지점은 "추천"이 아니라 **"대신 신청"**입니다. 활동을 골라주는 서비스는 여럿 있지만, 회원 대신 접수 절차를 진행하는 곳은 드뭅니다.

---

## 3. 무엇을 하는가 (3단계)

### (1) 말로 찾기
- 마이크를 누르고 말하거나 글로 적으면 됩니다. 검색어를 고민할 필요가 없습니다.
- 예: "이번 주에 배울 만한 거 있어?" · "집 근처에서 할 수 있는 운동 알려줘"
- 두서없이 말해도 의도를 해석합니다. 여러 AI 에이전트가 협력해 질문을 이해하고 후보를 좁힙니다.

### (2) 딱 맞는 3개 고르기
- 주민센터·복지관·도서관·문화센터·평생학습관·지자체 등 **흩어져 있는 기관의 프로그램을 한곳에 모아** 둡니다.
- 수십 개를 늘어놓지 않고, 관심사와 사는 곳에 맞는 **3개**를 고릅니다.
- 날짜·장소·수강료·신청 방법을 한 화면에서 볼 수 있습니다.

### (3) 신청 대행 — 여기가 핵심이자, 정직하게 말해야 하는 부분
${site.nameKo}이 직접 여는 강좌는 **접수까지 끝까지 마무리해 드립니다.**

외부 기관 강좌는 다릅니다. 신청에 필요한 정보를 미리 준비해 두고 로그인·신청서 작성까지 Agent가 진행하지만, **본인 확인(휴대폰 본인인증)처럼 회원 본인이 직접 해야 하는 단계**가 남습니다. 그 단계는 화면에서 안내해 드립니다. 기관에 따라 결제도 본인이 직접 하셔야 합니다.

즉 "모든 신청을 100% 자동으로 대신한다"는 표현은 **사실이 아니며, 우리는 그렇게 말하지 않습니다.** 회원의 개입이 필요한 지점이 어디인지 미리 보여 드리는 것이 설계 원칙입니다.

### (4) 다녀온 다음
- 다녀오면 도장을 찍고, 나만의 활동 달력이 쌓입니다.
- 신청 마감일과 수업 일정을 미리 알림으로 알려 드립니다.

---

## 4. 누구를 위한 것인가

- **주 사용자:** 55~70세 액티브 시니어 (한국). 은퇴 전후로 시간이 생겼고, 할 것을 찾고 있는 분들.
- **함께 쓰는 사람:** 부모님을 위해 대신 찾아 드리는 자녀 세대. 한 계정에서 가족을 위해 신청할 수 있습니다.
- **설계 원칙:** 글씨가 크고 버튼이 큽니다. 복잡한 설정이 없습니다. **AI는 제안하고, 결정은 언제나 사용자가 합니다.**
- **동의 원칙:** 회원 동의 없이는 어떤 신청도 진행하지 않습니다. 대신 신청할 때마다 무엇을 어디에 제출하는지 먼저 보여 드리고 확인받습니다.

---

## 5. 확인된 사실 (기준일 ${formatAsOf(asOf)})

- 활성 프로그램 카탈로그: **${count.toLocaleString('ko-KR')}건** (매일 갱신, 마감된 건은 자동 제외)
- 수집 대상 기관 유형: 주민센터 · 복지관 · 도서관 · 문화센터 · 평생학습관 · 백화점 문화센터 · 지자체 프로그램
- 서비스 지역: 대한민국
- 이용 언어: 한국어
- 설치와 활동 찾기: 무료
- 일부 신청 대행: 포인트가 사용되며, 사용 전에 화면에서 안내
- 안드로이드 앱: ${androidAppLive ? '출시됨' : '아직 출시 전 (iOS·웹으로 이용 가능)'}
- 문의: ${contact.email || site.contactEmail}

---

## 6. 어디서 받는가

${storeLines}

---

## 7. 자주 묻는 질문

${
    faq.length > 0
      ? faq.map((item) => `### ${item.q}\n\n${item.a}`).join('\n\n')
      : '(자주 묻는 질문을 불러오지 못했습니다. ' + site.url + '/product 에서 확인하실 수 있습니다.)'
  }

---

## 8. 회사

- 서비스명: ${site.nameKo}(${site.name}) · 웹사이트: ${site.url}
- 소개: ${site.url}/about · 기술(멀티 에이전트 구조): ${site.url}/technology
- 리서치: ${site.url}/research · 일하는 방식: ${site.url}/how-we-work
- 문의·기관 제휴: ${site.url}/contact · ${contact.email || site.contactEmail}
- 포지셔닝 한 줄: ${site.tagline}

${
    posts.length > 0
      ? `## 9. 우리가 쓴 글\n\n${posts
          .map((p) => `- [${p.title}](${site.url}/writing/${p.slug}): ${p.summary}`)
          .join('\n')}\n`
      : ''
  }
---

## 인용에 대하여

이 문서의 내용은 자유롭게 인용하셔도 됩니다. 인용하실 때 **${site.url}** 을 출처로 밝혀 주시면, 동명의 다른 서비스와 혼동되지 않습니다.
`;

  return new Response(body, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'public, max-age=0, s-maxage=86400, stale-while-revalidate=604800',
    },
  });
}
